{
  "name": "elm-css-normalize-test",
  "description": "Compare the generated and the original CSS files.",
  "version": "0.0.0",
  "dependencies": {
    "clean-css": "^4.1.4",
    "clean-css-cli": "^4.1.5"
  }
}
